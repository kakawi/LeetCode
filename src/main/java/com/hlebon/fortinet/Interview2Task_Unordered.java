package com.hlebon.fortinet;

/**
 * Input:
 * - array with size - n + 1
 * - contains numbers from 1 to n and 1 duplicate
 * - the array is unordered
 * Output:
 * - the duplicate
 */
public interface Interview2Task_Unordered {

  int findDuplicate(final int[] input);
}
